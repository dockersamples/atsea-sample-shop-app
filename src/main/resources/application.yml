---
server:
  port: 8080
  contextPath: /MobyStore
---
spring:
  profiles: dev
datasource:
  mobystore:
    url: jdbc:h2:~/test
    username: SA
    password:
    driverClassName: org.h2.Driver
    defaultSchema:
    maxPoolSize: 10
    hibernate:
      hbm2ddl.method: create-drop
      show_sql: true
      format_sql: true
      dialect: org.hibernate.dialect.H2Dialect
---
spring:
  profiles: postgres, default
datasource:
  mobystore:
    url: jdbc:postgresql://database:5432/mobystore
    username: gordonuser
    password: gordonpassword
    driverClassName: org.postgresql.Driver
    defaultSchema:
    maxPoolSize: 20
    hibernate:
      hbm2ddl.method: update
      show_sql: true
      format_sql: true
      dialect: org.hibernate.dialect.PostgreSQLDialect
---
spring:
  profiles: mysql
datasource:
  sampleapp:
    url: jdbc:mysql://database:3306/mobystore
    username: gordonuser
    password: gordonpassword
    driverClassName: com.mysql.jdbc.Driver
    defaultSchema:
    maxPoolSize: 20
    hibernate:
      hbm2ddl.method: update
      show_sql: true
      format_sql: true
      dialect: org.hibernate.dialect.MySQLDialect